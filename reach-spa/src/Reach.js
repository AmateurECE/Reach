import React from 'react';
import './Reach.css';
import Player from './Player';

function Reach() {
    return (
        <div className="App">
          <header className="App-header">
            <Player />
          </header>
        </div>
    );
}

export default Reach;
